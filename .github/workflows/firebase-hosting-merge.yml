# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
permissions:
  contents: 'read'
  id-token: 'write'

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup google cloud SDK
        uses: google-github-actions/setup-gcloud@v0
      - name: Authenticate to Google Cloud
        id: 'google-cloud-auth'
        uses: 'google-github-actions/auth@v0'
        with:
          create_credentials_file: true
          workload_identity_provider: 'projects/744209985918/locations/global/workloadIdentityPools/pool/providers/github-actions'
          service_account: 'github-action-474624755@lilydle-f1607.iam.gserviceaccount.com'
      - run: gcloud --quiet alpha storage cp  gs://lilydle-f1606.appspot.com/lilydle-answer.json ./src/data/lilydle-answer.json
      - run: yarn install --frozen-lockfile --non-interactive && yarn lint && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_LILYDLE_F1607 }}'
          channelId: live
          projectId: lilydle-f1607
